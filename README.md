# App 

Allows the user the upload their mood and optional note with the date logged

# features
Shows daily feed of mood
Captures each entry 
Resets each day

# link 


# create  virtual environemtn 
python -m venv venv

# activate virtual environment 
.\venv\Scripts\Activate.ps1

# install depedencies
pip install -r requirements.txt

# run app locally 
streamlit run app.py
